import { InscriptionClient } from "@/components/auth/InscriptionClient";

export const metadata = { title: "Créer un compte" };

export default function PageInscription() {
  return <InscriptionClient />;
}
